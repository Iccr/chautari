import 'package:flutter/material.dart';

class RoomView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Chautari Rents"),
      ),
      body: Center(
        child: Text("Room View"),
      ),
    );
  }
}
